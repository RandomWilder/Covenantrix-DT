appId: com.covenantrix.desktop
productName: Covenantrix
copyright: Copyright © 2025 ${author}

# CRITICAL: Proper file inclusion/exclusion
files:
  - dist/**/*
  - electron/**/*
  - resources/**/*
  - package.json
  - "!**/*.ts"
  - "!**/*.map"
  - "!src"
  - "!public"
  - "!node_modules/**/{CHANGELOG.md,README.md,LICENSE,*.txt}"
  - "!node_modules/**/{test,__tests__,tests,powered-test,example,examples}"
  - "!node_modules/**/{*.d.ts,*.md,*.markdown}"
  - "!node_modules/**/{tsconfig.json,tslint.json,eslint*,prettier*}"
  - "!node_modules/**/{.github,.vscode,.idea}"
  - "!**/{.git,.gitignore,.gitattributes}"
  - "!**/{.DS_Store,Thumbs.db}"

# Use asar for better compression
asar: true
asarUnpack:
  - resources/backend/**/*

directories:
  buildResources: build
  output: dist

# Compression for smaller installers
compression: maximum

# ============================================
# WINDOWS CONFIGURATION
# ============================================
win:
  target:
    - target: nsis
      arch:
        - x64
  icon: build/icon.ico
  artifactName: ${productName}-Setup-${version}.${ext}
  
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: ${productName}
  installerIcon: build/icon.ico
  uninstallerIcon: build/icon.ico
  license: LICENSE
  deleteAppDataOnUninstall: true
  # Compression for smaller installer
  packElevateHelper: false
  perMachine: false
  unicode: true

# ============================================
# MACOS CONFIGURATION
# ============================================
mac:
  target:
    - target: dmg
      arch:
        - universal
  icon: build/icon.icns
  category: public.app-category.productivity
  hardenedRuntime: true
  gatekeeperAssess: false
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
  artifactName: ${productName}-${version}.${ext}
  # Sign for distribution (requires Apple Developer account)
  # identity: "Developer ID Application: Your Name (TEAM_ID)"
  
dmg:
  contents:
    - x: 130
      y: 220
    - x: 410
      y: 220
      type: link
      path: /Applications
  sign: false
  background: build/background.png
  window:
    width: 540
    height: 380

# ============================================
# LINUX CONFIGURATION
# ============================================
linux:
  target:
    - target: AppImage
      arch:
        - x64
  icon: build/icon.png
  category: Office
  artifactName: ${productName}-${version}.${ext}
  synopsis: AI-powered document analysis assistant
  description: Covenantrix is an AI-powered desktop application that helps you analyze and understand your documents using advanced RAG technology.
  
appImage:
  license: LICENSE
  synopsis: AI-powered document analysis
  category: Office

# ============================================
# AUTO-UPDATE CONFIGURATION
# ============================================
publish:
  provider: github
  owner: RandomWilder
  repo: covenantrix-desktop
  releaseType: release
  publishAutoUpdate: true

# ============================================
# PROTOCOLS (for deep linking if needed)
# ============================================
protocols:
  name: covenantrix
  schemes:
    - covenantrix

# ============================================
# BUILD RESOURCES
# ============================================
extraResources:
  - from: resources
    to: resources
    filter:
      - "**/*"